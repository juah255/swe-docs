# Frontend Interview Questions

Comprehensive index of all mid/senior interview questions across frontend topics.
Answers are in the referenced source files.

## HTML and CSS

*Source:* `frontend/html-css.md`

- What is the difference between inline, block, and inline-block elements?
- How does the cascade and specificity work in CSS?
- When would you use Grid over Flexbox?
- What is the stacking context and how do you create one?
- What are container queries and when are they useful?
- What is the difference between `rem` and `em`?

## JavaScript

*Source:* `frontend/javascript.md`

- How does the event loop handle async operations?
- What is the difference between `var`, `let`, and `const`?
- How do closures affect memory?
- What is the difference between `==` and `===`?
- How do you deep clone an object?
- What is debouncing and throttling and when do you use each?

## State Management

*Source:* `frontend/state-management.md`

- How do you design frontend state management?
- When would you use Context vs a dedicated state library?
- How do you handle stale server state?
- What is the difference between controlled and uncontrolled components?
- What is prop drilling and how do you solve it?

## Performance

*Source:* `frontend/performance.md`

- How do you improve frontend performance?
- What is the difference between `preload`, `prefetch`, and `preconnect`?
- How do you measure real-user performance?
- What causes layout shifts and how do you prevent them?
- When should you use server-side rendering over client-side rendering?

## Accessibility

*Source:* `frontend/accessibility.md`

- What accessibility concerns should every frontend engineer understand?
- How do you test for accessibility?
- What is the difference between `aria-label` and `aria-labelledby`?
- How do you handle focus management in a single-page app?

## Build and Tooling

*Source:* `frontend/build-and-tooling.md`

- What is tree shaking and how does it work?
- What is the difference between dev dependencies and regular dependencies?
- How does hot module replacement work?
- When would you use a monorepo for frontend projects?

## Testing

*Source:* `frontend/testing.md`

- Which tests should run on every pull request?
- How do you test asynchronous behavior in frontend code?
- What is the difference between a mock, a stub, and a spy?
- How do you test component behavior without testing implementation details?

## Security

*Source:* `frontend/security.md`

- What are common frontend security risks?
- How does a Content Security Policy protect against XSS?
- What is the safest way to store authentication tokens in a browser?
- How do you protect against supply chain attacks in frontend builds?
