# Concurrency Questions

## Basic Questions

- What is concurrency?
- What is the difference between concurrency and parallelism?
- What is a race condition?
- What is deadlock?
- What is thread safety?
- What is a critical section?

## Synchronization Questions

- What is a mutex?
- What is a semaphore?
- What is the difference between a mutex and a semaphore?
- Why do we need synchronization?
- What problems happen when shared state is not synchronized?

## Execution Model Questions

- What is the difference between a thread and a process?
- What is the difference between synchronous and asynchronous programming?
- What is the difference between blocking and non-blocking I/O?
- When would you use threads instead of async?
- When would you use async instead of threads?

## Backend Scenario Questions

- How can two requests create a race condition in a backend application?
- How would you protect shared in-memory data from concurrent writes?
- How can database transactions help with concurrency issues?
- Why can a high-traffic server still fail even when it uses async code?
- How would you debug a deadlock in production?
