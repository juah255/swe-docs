# API and Communication Questions

## Basic Questions

- What is a REST API?
- What does stateless mean in HTTP?
- What is the difference between `GET` and `POST`?
- What is the difference between `PUT` and `PATCH`?
- What is the difference between `401 Unauthorized` and `403 Forbidden`?
- What is the difference between authentication and authorization?

## Browser and Client Questions

- When you log in to a website, which HTTP methods are commonly used?
- You can send an API request from Postman but cannot from the browser. What is the reason?
- What is CORS and why does the browser enforce it but Postman does not?
- What is the difference between cookies, local storage, and session storage?

## Token and Security Questions

- What is JWT?
- Why is JWT URL-safe?
- What is the difference between access token and refresh token?
- Why should sensitive data not be stored directly in a JWT payload?
- What is RBAC?
- What is OAuth?

## Backend Architecture Questions

- What is middleware?
- What is the role of a controller?
- What is the difference between a service and a repository?
- What is a DTO and why is it useful?
- What is business logic?
