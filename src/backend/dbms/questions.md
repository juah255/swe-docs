# DBMS Questions

## Basic Questions

- What is DBMS?
- What is the difference between DBMS and RDBMS?
- What is the difference between SQL and NoSQL databases?
- What is a primary key?
- What is a foreign key?
- What is a composite key?
- What is the difference between `DELETE`, `TRUNCATE`, and `DROP`?

## SQL Questions

- What is the difference between `WHERE` and `HAVING`?
- What is the difference between `GROUP BY` and `ORDER BY`?
- What is the difference between `UNION` and `UNION ALL`?
- What is the difference between `INNER JOIN`, `LEFT JOIN`, `RIGHT JOIN`, and `FULL JOIN`?
- What is a subquery?
- What is the difference between a subquery and a join?
- What is a view?
- What is a stored procedure?

## Normalization Questions

- What is normalization?
- Why is normalization important?
- What is denormalization?
- What is the difference between 1NF, 2NF, and 3NF?
- When would you choose denormalization instead of normalization?

## Indexing Questions

- What is an index?
- Why does an index improve query performance?
- What are the trade-offs of using indexes?
- What is the difference between clustered and non-clustered indexes?
- Why can too many indexes hurt performance?
- In which cases might the database not use an index?

## Transaction Questions

- What is a transaction?
- What are the ACID properties?
- What is the difference between `COMMIT` and `ROLLBACK`?
- What is transaction isolation level?
- What is dirty read?
- What is non-repeatable read?
- What is phantom read?
- What is deadlock?

## Interview Scenarios

- How would you optimize a slow query?
- How would you design a table for users and orders?
- How would you handle duplicate data in a table?
- How would you find the second highest salary from an employee table?
- How would you paginate a large result set efficiently?
- How would you choose between SQL and NoSQL for a new project?
