server {
    listen 80{% if listen_default_server %} default_server{% endif %};
    listen [::]:80{% if listen_default_server %} default_server{% endif %};
    server_name {{ server_name }};

    root {{ web_root }};
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
